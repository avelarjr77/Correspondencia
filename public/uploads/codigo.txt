$('#HolaRebeca').on('click',function(){
            // get data from button edit
            console.log("entró");
            /* var idR = $(this).data('idr');
            var nombreM = $(this).data('nombrer');
            // Set data to Form Edit
            $('.rolModuloMenuId').val(idR);
            $('.menuM').html(nombreM);
            // Call Modal Edit
            $('#eliminarModal').modal('show'); */
        });